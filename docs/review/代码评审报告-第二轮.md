# 代码评审报告 — 第二轮（测试+Review）

> 评审时间：2025-07-17  
> 评审范围：全量代码（后端 + 前端）  
> 评审人：后端架构师 Agent + 前端架构师 Agent

---

## 一、总体评分

| 维度    | 后端    | 前端    | 说明                     |
| ----- | ----- | ----- | ---------------------- |
| 编译/构建 | ✅ A   | ✅ A   | 均通过                    |
| 功能完整性 | B+    | B+    | 核心功能完成，License 吊销缺失    |
| 安全性   | **D** | B     | 后端 3 个硬编码密钥 + SQL 注入风险 |
| 代码质量  | B     | B     | 部分 Service 过长，前端有静默错误  |
| 测试覆盖  | **F** | **F** | 零单元测试                  |



---

## 二、问题清单

### 🔴 阻断级（必须修，共 6 项）

| # | 模块 | 问题                                                                                 | 位置                                                |
| - | -- | ---------------------------------------------------------------------------------- | ------------------------------------------------- |
| 1 | 后端 | **JWT 密钥硬编码** — `application.yml` 明文存储，任何人拿到代码仓库即可伪造 Token                         | `application.yml:69` + `JwtTokenProvider.java:20` |
| 2 | 后端 | **AES 密钥硬编码** — `xuhuan2021__mms!` 写在 LicenseGenerator 中，且使用 ECB 模式                | `LicenseGenerator.java:66`                        |
| 3 | 后端 | **RSA 私钥硬编码** — 测试私钥在源码中，无生产环境替换机制                                                 | `LicenseGenerator.java:24-53`                     |
| 4 | 后端 | **SQL 注入风险** — `PackageService.getPackageRecords` 使用 `.last("LIMIT ...")` 手动拼接 SQL | `PackageService.java:338`                         |
| 5 | 后端 | **密码明文存库** — ZIP 加密密码明文存储在 `package_task` 表，JVM 崩溃则永久残留                            | `PackageTask.java` + `PackageService.java`        |
| 6 | 后端 | **异常信息泄露** — `GlobalExceptionHandler` 将 `e.getMessage()` 直接返回给客户端                  | `GlobalExceptionHandler.java:44-53`               |

### 🟡 警告级（建议修，共 10 项）

| #  | 模块 | 问题                                                       | 位置                          |
| -- | -- | -------------------------------------------------------- | --------------------------- |
| 7  | 后端 | **JWT 过期 24h 偏长**，无 Refresh Token 机制                     | `application.yml:70`        |
| 8  | 后端 | **JWT 无黑名单**，logout 后 Token 仍可用                          | `AuthController.java:48-53` |
| 9  | 后端 | **CORS 过于宽松** — `localhost:*` + `allowCredentials(true)` | `SecurityConfig.java:59-65` |
| 10 | 后端 | **缺少 Rate Limiting**，登录接口可被暴力破解                          | 全局                          |
| 11 | 后端 | **密码修改缺少权限校验** — 普通用户可修改任意用户密码                           | `UserController.java:55-56` |
| 12 | 后端 | **License 缺少吊销功能** — 无法提前终止 License                      | `LicenseController.java`    |
| 13 | 后端 | **异步任务异常路径 encrypted 始终为 false** — 丢失用户加密意愿              | `PackageService.java:251`   |
| 14 | 前端 | **大量 catch 块静默失败** — 只有 `console.error`，用户无感知            | 几乎所有视图文件                    |
| 15 | 前端 | **API 双重解包风险** — `request.js` 已解包但视图又 `res?.data`        | 多个视图                        |
| 16 | 前端 | **License 下载 URL 可能重复 `/api` 前缀**                        | `api/license.js`            |

### 🔵 建议级（可选修，共 8 项）

| #  | 模块 | 问题                             |
| -- | -- | ------------------------------ |
| 17 | 后端 | 零单元测试覆盖                        |
| 18 | 后端 | API 路径风格不统一（单复数混用）             |
| 19 | 后端 | 魔法字符串过多，缺少 LicenseStatus 枚举    |
| 20 | 后端 | 分片上传 session 无过期清理机制           |
| 21 | 后端 | 超大文件解压 OOM 风险（整个 tar 条目读入内存）   |
| 22 | 前端 | InfraTabContent.vue 是死代码，未被使用  |
| 23 | 前端 | 进度轮询 setInterval 在 SPA 导航时可能泄漏 |
| 24 | 前端 | 用户管理密码无复杂度校验                   |

---

## 三、优先修复路线图

### 🔴 立即修复（安全红线）

1. 所有硬编码密钥 → 环境变量注入（#1, #2, #3）
2. SQL 注入 → MyBatis-Plus 标准分页（#4）
3. 密码不持久化 → 内存 ConcurrentHashMap（#5）
4. 异常信息脱敏 → 生产环境返回通用消息（#6）

### 🟡 本周修复

1. 添加登录频率限制（#10）
2. 密码修改接口权限校验（#11）
3. License 吊销功能（#12）
4. 前端静默错误 → ElMessage.error（#14）

### 🔵 迭代优化

1. 单元测试（#17）
2. 统一 API 路径 + 枚举替换魔法字符串（#18, #19）
3. 前端代码复用优化（#22, #23）

---

## 四、已验证通过项

| 检查项                            | 状态    |
| ------------------------------ | ----- |
| 编译通过（后端 mvn compile）           | ✅     |
| ResourceUpload.vue div 平衡      | ✅ (0) |
| LicenseManagement.vue div 平衡   | ✅ (0) |
| UserManagement.vue div 平衡      | ✅ (0) |
| ProjectList.vue div 平衡         | ✅ (0) |
| AppHeader.vue div 平衡           | ✅ (0) |
| ZIP 加密可选功能（前后端）                | ✅     |
| Redis 全链路（前后端）                 | ✅     |
| 密码存储 DelegatingPasswordEncoder | ✅     |
| Spring Security 循环依赖已修复        | ✅     |
| 管理员密码 hash 正确                  | ✅     |
